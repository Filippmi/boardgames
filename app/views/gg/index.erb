<div class="center">
    <h3>Your Game List</h3>
</div>


<div class="center">
    <ul>
        <% @games.each do |game| %>
            <li><a href="/my_games/<%= game.id %>"><%= game.title %></a></li>
        <%end%>
    </ul>
</div>

<p>Last Veiwed: <%= @game ? @game.title : "You have not veiwed anything recently." %></p>